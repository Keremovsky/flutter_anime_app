import 'package:flutter/material.dart';

class ListsPageView extends StatelessWidget {
  const ListsPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          height: 100,
          width: 100,
          color: Colors.purple,
        )
      ],
    );
  }
}
