import 'package:flutter/material.dart';

class WatchingListPageView extends StatelessWidget {
  const WatchingListPageView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
          height: 100,
          width: 100,
          color: Colors.blue,
        )
      ],
    );
  }
}
